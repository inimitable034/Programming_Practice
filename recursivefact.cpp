#include <iostream>
using namespace std;
	    
	    int check(int arr[a],int arr[b])
	    {
	    	while(arr[a])
	    	if( arr[i]<arr[i+1])
	    	return check( arr[i+1],arr[i+2]);
		}

int main() {
	int arr[100];
	cout<<"enter size of the array :\n";
	cin>>n;
	cout<<"enter items the array :\n";
	{
	for(i=0;i<n;i++)
	cin>>arr[i];
	}
	check(arr[i],arr[i+1]);
    return 0;
}
